# Supabase. Public by design — these reach the client and RLS is what protects
# the data behind them.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Gemini. Server only. Never prefix it with VITE_ or it is inlined into the
# client bundle, where an API key is public.
GEMINI_API_KEY=
