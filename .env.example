# Supabase (público — se expone al cliente, protegido por RLS)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Gemini — SOLO server. Nunca con prefijo VITE_ o termina en el bundle del cliente.
GEMINI_API_KEY=
